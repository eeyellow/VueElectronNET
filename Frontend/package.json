{
  "name": "vueapp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run sync && vite --host",
    "build": "npm run sync && vite build --emptyOutDir",
    "preview": "npm run sync && vite preview",
    "sync": "node sync.js"
  },
  "dependencies": {
    "vue": "^3.4.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.2.0"
  }
}
